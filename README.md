# FPSValidator

Metal ベースの iOS FPS 計測・検証アプリです。指定フレームレートの生成と実測を行い、表示の安定性を確認できます。

## 構成

- `FPSValidator/` — Xcode プロジェクトとアプリ本体
- `.github/workflows/build-ipa.yml` — IPA ビルド

## ビルド

```bash
open FPSValidator/FPSValidator.xcodeproj
```
